spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:db;MODE=MSSQLSERVER;DB_CLOSE_DELAY=-1
    username: sa
  jpa:
    properties:
      hibernate:
        default_schema: uhta